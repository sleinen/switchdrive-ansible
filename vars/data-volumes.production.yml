---

# which ceph volumes to snapshot
snapshot_volumes:

ocdata_links:
   - { name: ".ocdata1", link: "data" }
   - { name: ".ocdata2", link: "231" }
   - { name: ".ocdata3", link: "119" }

nfs_volumes:
  - ip: "{{nfs1_ip}}"
    volumes: "{{os_volumes.nfs1}}"
  - ip: "{{nfs2_ip}}"
    volumes: "{{os_volumes.nfs2}}"
  - ip: "{{nfs3_ip}}"
    volumes: "{{os_volumes.nfs3}}"
  - ip: "{{nfs4_ip}}"
    volumes: "{{os_volumes.nfs4}}"
  - ip: "{{nfs5_ip}}"
    volumes: "{{os_volumes.nfs5}}"
  - ip: "{{nfs6_ip}}"
    volumes: "{{os_volumes.nfs6}}"
  #- ip: "{{nfs7_ip}}"
  #  volumes: "{{os_volumes.nfs7}}"
